@page
@model OdeToFood.Pages.Restaurants.ListModel
@{
    ViewData["Title"] = "List";
}

<h1>Restaurants</h1>

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="input-group-btn">
                <button class="btn btn-dark">
                    <i>search</i>
                </button>
            </span>
        </div>
    </div>
</form>

@*<table class="table">
    @foreach (var restaurant in Model.Restaurants)
    {
    <tr>
        <td>@restaurant.Name</td>
        <td>@restaurant.Location</td>
        <td>@restaurant.Cuisine</td>
        <td>
            <a class="btn btn-dark" asp-page="./Detail" asp-route-restaurantId="@restaurant.Id">details</a>
        </td>
        <td>
            <a class="btn btn-dark" asp-page="./Edit" asp-route-restaurantId="@restaurant.Id">edit</a>
        </td>
        <td>
            <a class="btn btn-dark" asp-page="./Delete" asp-route-restaurantId="@restaurant.Id">delete</a>
        </td>
    </tr>
    }
</table>*@

@foreach (var restaurant in Model.Restaurants)
{
    <partial name="_Summary" model="restaurant" />
}

<a asp-page="./Edit" class="btn btn-dark">Add new</a>

@section footer {
    <div>@Model.Message</div>
}


